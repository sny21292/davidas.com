// The @modal slot is empty unless an intercepting route fills it (i.e. on a
// direct visit to /showcase/[slug], or anywhere other than a soft in-app click).
export default function ModalDefault() {
  return null;
}
